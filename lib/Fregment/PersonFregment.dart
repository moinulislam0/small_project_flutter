import 'package:flutter/cupertino.dart';

class PersonFregment extends StatelessWidget{
  @override
  Widget build(BuildContext context) {

    return Container(
      child: Center(
        child: Text("PersonFregment"),
      ),
    );
  }
}
import 'package:flutter/cupertino.dart';

class SettingFregment extends StatelessWidget{
  @override
  Widget build(BuildContext context) {

    return Container(
      child: Center(
        child: Text("SettingFregment"),
      ),
    );
  }
}
import 'package:flutter/cupertino.dart';

class SearchFregment extends StatelessWidget{
  @override
  Widget build(BuildContext context) {

    return Container(
      child: Center(
        child: Text("SearchFregment"),
      ),
    );
  }
}